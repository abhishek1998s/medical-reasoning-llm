# Medical Reasoning LLM — Dependencies
#
# Lesson learned on Day 1: pinning the GPU stack to specific old versions
# breaks on Kaggle's current CUDA 12.8 (libbitsandbytes_cuda128.so missing,
# GLIBCXX_3.4.32 not found). We now use minimum-version constraints for
# the GPU stack and let Kaggle/Colab keep its preinstalled compatible
# versions.
#
# Day-2/3 training notebooks should:
#   1. Verify imports succeed (Notebook 01 cell 3 pattern)
#   2. ONLY pip-install if a verify fails — `pip install -q --upgrade <pkg>`
#   3. Log actually-installed versions to outputs/<track>/training_meta.json

# --- Core training stack: GPU-coupled, let runtime pick compatible versions ---
unsloth>=2025.1
transformers>=4.46
trl>=0.12
peft>=0.13
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.45

# --- Experiment tracking ---
wandb>=0.18

# --- Evaluation: pure-Python, stable APIs, safe to pin ---
evaluate==0.4.3
rouge-score==0.1.2
sacrebleu==2.4.3
bert-score==0.3.13
sentence-transformers>=3.0     # has GPU dep, range like core stack

# --- LLM-as-judge clients ---
openai>=1.50            # works for Groq via base_url
google-genai>=0.3       # used only if Gemini becomes a third judge

# --- General-purpose, safe to pin ---
pandas>=2.0
numpy>=1.26             # Kaggle 2026 has numpy 2.x; 1.26 was breaking other libs
PyYAML==6.0.2
tqdm==4.67.1

# --- Test framework (local laptop dev) ---
pytest>=8.0

# Local-only convenience: install transformers + pytest for src/ tests
# without pulling the GPU stack:
#     pip install transformers pytest
